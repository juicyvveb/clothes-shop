
// console.log(document.querySelector('.container-error'))
    // let error = document.querySelector('.container-error')
    // console.log(error)
    // // pageWidth = window.innerWidth,
    // // pageHeight = window.innerHeight,
    // // errorWidth = error.offsetWidth,
    // // errorHeight = error.offsetHeight;

    // // error.style.top = (pageHeight / 2) - (errorHeight / 2) + "px";
    // // error.style.left = (pageWidth / 2) - (errorWidth / 2) + "px";
